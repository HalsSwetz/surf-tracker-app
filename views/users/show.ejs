<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/user.css" />
    <title><%= user.username %></title>
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>
  <h1><%= foundUser.username %>'s Waves</h1>
  <h2>Waves:</h2>
  <ul class="wave-list">
    <% waves.forEach(wave => { %>
    <li>
        <a href="/users/<%= foundUser._id %>/waves/<%= foundUser._id %>/<%= wave._id %>">
        <%= wave.name %>
        </a>
    </li>
<% }) %>
</ul>
 
  
</body>
</html>