<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/profile.css" />
    <title>Edit Profile</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1 class="profile-header">Edit Profile</h1>
    <div class="profile-update">
    <form action="/profiles/<%= profile._id %>?_method=PUT"method="POST">
    <div class="profile-update-info">
        <label for="picture">Picture Url:</label>
        <input type="text" id="picture" name="picture" value="<%= profile.picture %>">

        <label for="bio">Bio:</label>
        <textarea id="bio" name="bio"><%= profile.bio %></textarea>

      <button class="update-profile-button" type="submit">Update profile</button>
    
      </div>
    </div>
</body>
</html>